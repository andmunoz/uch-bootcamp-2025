CREATE TABLE Task (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    completed INTEGER NOT NULL DEFAULT 0,
    userId INTEGER
);

selectAll:
SELECT * FROM Task;

insertTask:
INSERT OR REPLACE INTO Task (title) VALUES (?);

removeTask:
DELETE FROM Task WHERE id = ?;

updateTask:
UPDATE Task SET title = ? WHERE id = ?;

