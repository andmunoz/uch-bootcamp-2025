package cl.uchile.postgrado.mobile.shoppinglist.model.room

import cl.uchile.postgrado.mobile.shoppinglist.model.data.ShoppingListData

class ShoppingListRepository(private val database: ShoppingListDatabase) {
    // val shoppingLists: List<ShoppingListData> = database.shoppingListDAO().getShoppingLists()
}
